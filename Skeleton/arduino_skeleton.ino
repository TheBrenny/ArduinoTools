/**
 * Called at the very start when the arduino is turned on.
 */
void setup() {}

/**
 * Called continuously - what needs to be repeated?
 */
void loop() {}

/**
 * When the loop void is finished, there is a check to test the availability of
 * read data from the serial. If there is, this method is called.
 */
void serialEvent() {}